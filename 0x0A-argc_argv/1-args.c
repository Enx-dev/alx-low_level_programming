#include <stdio.h>
/**
 * main - prints the number of agur passed
 * @argc: length of the aguments
 * @argv: array of the agumentes
 *
 * Return: int
*/
int main(int argc, char *argv[])
{
	(void)argv;
	printf("%d\n", argc);
	return (0);
}
